
describe('babel/bin/babel-build', () => {
  // const argv = cli(cwd).parse(['args']);
  afterEach(() => {
    jest.resetModules();
  })

  it('needs tests', () => {
    const babelBuild = require('../babel-build');
  });
});
